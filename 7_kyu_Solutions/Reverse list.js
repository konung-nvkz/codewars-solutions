/* Reverse list
DESCRIPTION:
Write reverseList function that simply reverses lists.
*/
//JS
function reverseList(arr) {
    return arr.reverse(); // use reverse method
}

/*Python:
    def reverse_list(lst):
        return list(reversed(lst))
*/